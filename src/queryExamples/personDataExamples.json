{
    "examples": [{
            "header": "JSON",
            "examples": []
        },
        {
            "header": "RDF",
            "examples": ["QUERY (\\triple total -> if tripleContainsString triple (Just \"Renaud_de_Cormont\") Nothing Nothing then cons total triple else total)\nFROM personGraph\nTO rdf",
            "QUERY (\\x newrdf -> if length (triplesOf newrdf) < 5 then cons newrdf x else newrdf)\nFROM personGraph\nTO rdf"]
        },
        {
            "header": "JSON + RDF",
            "examples": []
        }
    ]
}